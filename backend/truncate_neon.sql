-- Truncate all data tables in Neon (preserves schema, removes all data)
-- This allows for a clean re-import
TRUNCATE TABLE 
    users CASCADE,
    assessment_answers CASCADE,
    assessment_questions CASCADE,
    assessments CASCADE,
    career_recommendations CASCADE,
    cover_letters CASCADE,
    industry_insights CASCADE,
    branch_industry_insights CASCADE,
    student_industry_insights CASCADE,
    learning_paths CASCADE,
    learning_skills CASCADE,
    mentor_profiles CASCADE,
    resumes CASCADE,
    resume_review_requests CASCADE,
    cover_letter_review_requests CASCADE,
    selected_careers CASCADE,
    skill_completions CASCADE,
    student_assessments CASCADE,
    student_learning_progress CASCADE,
    student_profiles CASCADE,
    user_activity CASCADE,
    courses CASCADE,
    skill_test_questions CASCADE,
    mentor_courses CASCADE,
    sessions CASCADE,
    payments CASCADE,
    course_enrollments CASCADE,
    admin_ledger CASCADE,
    session_messages CASCADE,
    assignments CASCADE,
    assignment_submissions CASCADE,
    career_nav_ratings CASCADE,
    plans CASCADE,
    plan_topics CASCADE,
    session_schedule CASCADE,
    session_resources CASCADE,
    mentor_experiences CASCADE,
    mentor_tests CASCADE,
    mentor_course_verifications CASCADE,
    notifications CASCADE
RESTART IDENTITY;

